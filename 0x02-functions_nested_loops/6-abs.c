#include "main.h"

/**
 * _abs - computes the absolute value
 *
 * @int: inputs character
 *
 * Return: 0 Success
 */

int _abs(int)
{
	int k;

	if (k >= 0)
	{
	return (k);
	}
	else
	{
	return (k * -1);
	}
}
